# Smoke config: code-mode QA on the qa_2400 stems (12 records × 1 model).
# Populate test_data_img/ first:
#   uv run python tools/download_qa_img.py --out test_data_img --limit 12
# Same fixture as test_img.yaml, but mode=code → side-by-side code-vs-img comparison.
# Paths are relative to QA/.

mode:     code
data_dir: test_data_img
out_dir:  results_test_code

models:
  - gpt-4o
